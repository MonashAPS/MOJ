# @moj/core

Pure domain logic with no I/O and no runtime dependencies: permissions, contest formats, contest timing,
ratings, points, verdicts, judging state and scoreboard freezing. The same functions run in Convex, in
Next.js server components and in unit tests.
